import 'package:meta/meta.dart';

/// Model [AuthModel] : Model for taking username and password
class AuthModel {
  final String username;
  final String password;

  AuthModel({@required this.username, @required this.password});
}
